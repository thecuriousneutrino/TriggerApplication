name: Docker Image CI

on: 
  push:
    branches:
    - Citest
      
jobs:
  container:
    runs-on: ubuntu-latest
    container: docker://hkdaq/triggerapplication:base
    steps:
      - uses: actions/checkout@v1
      - run: | 
         ls ./ -al
         git branch
         git checkout Application
         make clean
         make
         
          
